<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.stayfit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivities.ReportActivity"></activity>
        <activity android:name=".MainActivities.FirstActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivities.WorkoutActivity" />
        <activity android:name=".HomeActivities.HomeActivity" />
        <activity android:name=".HomeActivities.Stopwatch"/>
        <activity android:name=".MainActivities.GoalActivity" />
        <activity android:name=".MainActivities.SignupActivity" />
        <activity android:name=".MainActivities.MainActivity" />

    </application>

</manifest>